const sum = (a, b) => a + b;

// Each file has its own scope
// This file by default is not accesed from other file
// We need to link those files

// #MODULES
// JavaScript modules have couple of rules and systems
